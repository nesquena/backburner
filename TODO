Job should be:

class Foo
  include Echelon::Job
end

We should allow

class User
  include Echelon::PerformableObject
end

should allow

User.delay.some_method("foo", "bar")

or

User.find(1).delay.some_method("foo", "bar")

and also

Echelon::Worker.enqueue Foo, { :foo => "bar", :bar => "baz" }

Should do:

Foo.new(:foo => "bar", :bar => "baz").perform

and:

    User.find(1).delay.some_method("foo", "bar")

is

    Echelon::Worker.enqueue Echelon::PerformableMethod, { :klass => User, :id => 1, :method => "some_method", :args => [] }