#!/usr/bin/env ruby

require 'echelon'
require 'dante'

# bundle exec echelon foo,bar
queues = (ARGV.first ? ARGV.first.split(',') : nil) rescue nil
Dante.run('echelon') do |opts|
  Echelon.work!(queues)
end